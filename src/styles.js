import { Padding } from "@mui/icons-material";
import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  root: {
    display: "flex",
    height: "100%",
  },
  content: {
    flexGrow: "1",
    Padding: "2em",
  },
  toolbar: {
    height: "70px",
  },
}));
